application.name = "StoreManager-scala"

akka {
level = DEBUG
}

http {
interface = "0.0.0.0"
port = 8080
}

database {
  profile = "slick.jdbc.PostgresProfile$"
  driver = org.postgresql.Driver
  databaseName = "sample"
  url = "jdbc:postgresql://localhost:5432/storemanager_scala"
  url = ${?PSQL_URL}
  user = "allanmogusu"
  user = ${?PSQL_USER}
  password = ""
  password = ${?PSQL_PASSWORD}
  driver = org.postgresql.Driver
  numThreads = 10
}

